pipeline {    
    options { skipDefaultCheckout() }
     agent {label 'master'} 
      
    stages { 
     stage('SCM') {
            steps {
                git  branch: 'main', url: 'git@github.com:AhmedRomdhani16/PROJECT-CI.git'
            }
        }
        
}
}
