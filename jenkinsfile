pipeline {    
    options { skipDefaultCheckout() }
     agent {label 'master'} 
      
    stages { 
     stage('SCM') {
            steps {
                git 'http://github.com/AhmedRomdhani16/PROJECT-CI.git'
                 
            }
        }
        
}
}
