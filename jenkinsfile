pipeline {    
    options { skipDefaultCheckout() }
     agent {label 'maître'} 
      
    stages { 
     stage('SCM') {
            steps {
                git  branch: 'main', url: 'https://github.com/AhmedRomdhani16/PROJECT-CI.git'
            }
        }
        
}
}
