pipeline {    
    options { skipDefaultCheckout() }
     agent {label 'master'} 
      
    stages { 
     stage('SCM') {
            steps {
               
                git  branch: 'main', url: 'ssh://github.com/AhmedRomdhani16/PROJECT-CI.git'
                 
            }
        }
        
}
}
